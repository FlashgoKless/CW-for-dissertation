# 06. Сценарии и потоки

## Сценарий 1: создание сессии и получение маршрута

```mermaid
sequenceDiagram
    actor Passenger as Пассажир
    participant Channel as Пользовательский канал
    participant API as API платформы
    participant Orchestrator as Сценарный оркестратор
    participant Ticket as Билетная система
    participant Schedule as Сервис расписания
    participant Navigation as Сервис навигации
    participant DB as База состояния

    Passenger->>Channel: Открывает сценарий поездки
    Channel->>API: POST /journey-sessions
    API->>Orchestrator: createSession(ticket_ref, entry_node)
    Orchestrator->>Ticket: Проверить ссылку на билет
    Ticket-->>Orchestrator: trip_id, station_id, departure_time
    Orchestrator->>Schedule: Получить актуальный статус рейса
    Schedule-->>Orchestrator: platform, status, updated_at
    Orchestrator->>Navigation: Рассчитать маршрут до платформы
    Navigation-->>Orchestrator: route_segments
    Orchestrator->>DB: Сохранить JourneySession, TripContext, ScenarioStep
    Orchestrator-->>API: Состояние, маршрут, подсказки
    API-->>Channel: 201 Created
    Channel-->>Passenger: Показывает маршрут и подсказки
```

### Ошибки

- Билет не найден: сессия не создается, канал получает код `ticket_not_found`.
- Рейс не найден в расписании: сессия создается в состоянии `needs_manual_check`, канал показывает ручной сценарий.
- Маршрут невозможен: сессия создается, но `ScenarioStep` получает статус `route_unavailable`.

## Сценарий 2: смена платформы

```mermaid
sequenceDiagram
    participant Schedule as Сервис расписания
    participant API as API платформы
    participant Orchestrator as Сценарный оркестратор
    participant EventLog as External event log
    participant Navigation as Сервис навигации
    participant DB as База состояния
    participant Queue as Брокер событий
    participant Worker as Worker уведомлений
    participant Delivery as Канал доставки

    Schedule->>API: POST /external-events/schedule
    API->>Orchestrator: handleTripPlatformChanged(event)
    Orchestrator->>EventLog: Проверить external_event_id
    EventLog-->>Orchestrator: Событие новое
    Orchestrator->>DB: Найти активные сессии рейса
    Orchestrator->>Navigation: Пересчитать маршруты
    Navigation-->>Orchestrator: Новые route_segments
    Orchestrator->>DB: Обновить TripContext, ScenarioStep, Hint
    Orchestrator->>Queue: Опубликовать hint.created
    Worker->>Queue: Получить hint.created
    Worker->>Delivery: Передать подсказку для доставки
    Delivery-->>Worker: Принято
    Worker->>DB: Сохранить NotificationDelivery
```

### Ошибки

- Событие пришло повторно: `ExternalEvent` помечается как duplicate, доменное состояние не меняется.
- Сервис навигации не может построить маршрут: создается подсказка с просьбой обратиться к сотруднику.
- Канал доставки недоступен: `NotificationDelivery` фиксируется как failed, подсказка остается доступна через `GET /hints`.

## Жизненный цикл JourneySession

```mermaid
stateDiagram-v2
    [*] --> created
    created --> resolving_ticket
    resolving_ticket --> active
    resolving_ticket --> rejected
    active --> route_ready
    route_ready --> waiting_boarding
    waiting_boarding --> route_changed
    route_changed --> waiting_boarding
    waiting_boarding --> completed
    active --> needs_manual_check
    route_ready --> needs_manual_check
    needs_manual_check --> active
    active --> expired
    waiting_boarding --> expired
    rejected --> [*]
    completed --> [*]
    expired --> [*]
```

## Жизненный цикл подсказки

```mermaid
stateDiagram-v2
    [*] --> created
    created --> queued
    queued --> delivered
    queued --> delivery_failed
    delivery_failed --> queued
    delivered --> acknowledged
    created --> suppressed
    acknowledged --> [*]
    suppressed --> [*]
```

## Асинхронные шаги

| Шаг | Почему асинхронный | Повторяемость |
|---|---|---|
| Обработка события расписания | События могут приходить независимо от запросов пассажира | Идемпотентно по `external_event_id` |
| Доставка подсказки | Канал доставки может быть недоступен | Повтор по `hint_id` и `delivery_attempt` |
| Истечение сессии | Выполняется по времени, а не по действию пассажира | Идемпотентно по `journey_session_id` |
| Обновление карты-графа | Новая версия карты применяется отдельно от активных запросов | Версионируется по `map_version` |

## Где меняется состояние

| Состояние | Кто меняет | Условие |
|---|---|---|
| `JourneySession.status` | Сценарный оркестратор | Команда канала, событие расписания, cleanup |
| `TripContext` | Сценарный оркестратор | Ответ расписания или событие изменения рейса |
| `ScenarioStep` | Сценарный оркестратор | Изменение рейса, маршрута или времени до отправления |
| `RouteSegment` | Сервис навигации через оркестратор | Новый маршрут или смена карты |
| `Hint.status` | Оркестратор и worker уведомлений | Создание, доставка, подтверждение |
| `ExternalEvent.status` | Обработчик внешних событий | Новое, обработанное, повторное, ошибочное |

## Правила идемпотентности

- `POST /journey-sessions` принимает `idempotency_key` от канала, если канал может повторить запрос.
- Каждое внешнее событие обязано иметь `external_event_id`, `source_system` и `event_type`.
- Повтор события с тем же `external_event_id` не вызывает повторный пересчет сценария.
- Доставка одной подсказки в один канал различается по `hint_id`, `channel_session_id` и номеру попытки.
- Завершение истекшей сессии можно повторять без изменения результата.

