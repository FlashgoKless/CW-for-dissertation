# 05. Архитектура

## Архитектурный стиль

MVP строится как модульная сервисная система с API-first границей и событийной интеграцией с внешними системами. Платформа хранит собственное состояние пассажирского сценария, но не становится источником истины для билетов, расписания или каналов доставки.

Ключевой принцип: внешние сервисы дают факты, а платформа превращает их в состояние `JourneySession`, маршруты и подсказки.

## Контейнеры

| Контейнер | Ответственность |
|---|---|
| API платформы | Принимает запросы внешних каналов, проверяет права доступа к сессии, возвращает сценарий, маршрут и подсказки |
| Сценарный оркестратор | Ведет состояние `JourneySession`, применяет сценарные правила, создает `Hint` |
| Сервис навигации | Хранит карту-граф в рабочем представлении и рассчитывает маршрут |
| Интеграционные адаптеры | Изолируют платформу от форматов билетной системы, расписания и канала доставки |
| Брокер событий | Передает события расписания, подсказок и истечения сессий между процессами |
| База состояния | Источник истины для сессий, контекста рейса, подсказок, внешних событий и аудита |
| Worker уведомлений | Обрабатывает созданные подсказки и вызывает внешний канал доставки |
| Планировщик cleanup | Завершает истекшие сессии и запускает очистку временных данных |

## Container Diagram

```mermaid
C4Container
    title Контейнерная диаграмма MVP

    Person(passenger, "Пассажир", "Использует внешний канал")
    Person(staff, "Сотрудник вокзала", "Смотрит состояние сценариев")
    Person(it, "IT-специалист", "Настраивает карту и интеграции")

    System_Ext(channel, "Пользовательский канал", "Мобильное приложение, сайт, киоск или табло")
    System_Ext(ticket, "Билетная система", "Источник истины для билета")
    System_Ext(schedule, "Сервис расписания", "Источник истины для рейса")
    System_Ext(delivery, "Канал доставки уведомлений", "Доставляет подсказки")
    System_Ext(obs, "Система наблюдаемости", "Логи, метрики, трассировка")

    System_Boundary(platform, "Платформа умного вокзала") {
        Container(api, "API платформы", "HTTP API", "Команды и запросы внешних каналов")
        Container(orchestrator, "Сценарный оркестратор", "Backend service", "Состояние сессии и сценарные правила")
        Container(navigation, "Сервис навигации", "Backend service", "Маршруты по карте-графу")
        Container(adapters, "Интеграционные адаптеры", "Backend module", "Вызовы внешних API и прием событий")
        Container(worker, "Worker уведомлений", "Background worker", "Доставка созданных подсказок")
        Container(cleanup, "Планировщик cleanup", "Background worker", "Истечение сессий и очистка")
        Container(queue, "Брокер событий", "Message broker", "Асинхронные события платформы")
        ContainerDb(db, "База состояния", "PostgreSQL", "Сессии, контекст рейса, подсказки, аудит")
    }

    Rel(passenger, channel, "Использует")
    Rel(channel, api, "HTTPS")
    Rel(staff, api, "HTTPS")
    Rel(it, api, "HTTPS")
    Rel(api, orchestrator, "Команды сценария")
    Rel(orchestrator, navigation, "Расчет маршрута")
    Rel(orchestrator, db, "Чтение и запись состояния")
    Rel(navigation, db, "Чтение карты-графа")
    Rel(orchestrator, queue, "Публикует события")
    Rel(worker, queue, "Получает события подсказок")
    Rel(cleanup, queue, "Публикует истечение сессий")
    Rel(worker, adapters, "Доставка подсказки")
    Rel(adapters, ticket, "API")
    Rel(adapters, schedule, "API и события")
    Rel(adapters, delivery, "API")
    Rel(api, obs, "Сигналы")
    Rel(orchestrator, obs, "Сигналы")
    Rel(worker, obs, "Сигналы")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

## Основные связи

| Откуда | Куда | Зачем |
|---|---|---|
| Пользовательский канал | API платформы | Создать или прочитать сессию |
| API платформы | Сценарный оркестратор | Выполнить команду сценария |
| Сценарный оркестратор | Сервис навигации | Рассчитать или пересчитать маршрут |
| Сценарный оркестратор | База состояния | Сохранить источник истины сценария |
| Сценарный оркестратор | Брокер событий | Опубликовать созданную подсказку |
| Worker уведомлений | Канал доставки уведомлений | Доставить подсказку во внешний канал |
| Интеграционные адаптеры | Билетная система и расписание | Получить внешние факты без протекания форматов внутрь домена |

## Component Diagram сценарного оркестратора

```mermaid
C4Component
    title Компоненты сценарного оркестратора

    Container_Boundary(orchestrator, "Сценарный оркестратор") {
        Component(commandHandler, "Command handlers", "Код", "Создание и изменение сессии")
        Component(eventHandler, "External event handlers", "Код", "Обработка событий расписания")
        Component(policy, "Scenario policy", "Код", "Правила переходов и подсказок")
        Component(routeFacade, "Route facade", "Код", "Запрос маршрутов у сервиса навигации")
        Component(hintFactory, "Hint factory", "Код", "Создание подсказок с причиной")
        Component(repository, "Journey repository", "Код", "Чтение и запись состояния")
        Component(eventLog, "External event log", "Код", "Идемпотентность внешних событий")
        Component(publisher, "Event publisher", "Код", "Публикация доменных событий")
    }

    ContainerDb(db, "База состояния", "PostgreSQL", "Состояние и аудит")
    Container(queue, "Брокер событий", "Message broker", "Доменные события")
    Container(navigation, "Сервис навигации", "Backend service", "Расчет маршрута")

    Rel(commandHandler, repository, "Загружает и сохраняет сессию")
    Rel(commandHandler, policy, "Применяет правила")
    Rel(eventHandler, eventLog, "Проверяет external_event_id")
    Rel(eventHandler, policy, "Пересчитывает сценарий")
    Rel(policy, routeFacade, "Запрашивает маршрут")
    Rel(routeFacade, navigation, "Расчет маршрута")
    Rel(policy, hintFactory, "Создает подсказки")
    Rel(hintFactory, publisher, "Публикует hint.created")
    Rel(repository, db, "SQL")
    Rel(eventLog, db, "SQL")
    Rel(publisher, queue, "Message")
```

## Ключевые политики

| Политика | Где реализуется | Почему здесь | Как проверить |
|---|---|---|---|
| Проверка доступа к сессии | API платформы | Все внешние запросы проходят через эту границу | Integration test |
| Сценарные переходы | Сценарный оркестратор | Только он владеет состоянием `JourneySession` | Unit tests политики |
| Идемпотентность внешних событий | `External event log` в оркестраторе | Повторы приходят до изменения доменного состояния | Failure test |
| Расчет маршрута | Сервис навигации | Маршрут зависит от карты-графа и правил доступности | Unit tests графа |
| Доставка подсказок | Worker уведомлений | Доставка может быть асинхронной и повторяемой | Integration test |
| Истечение сессии | Планировщик cleanup и оркестратор | Нужна централизованная политика retention | Integration test |

## Источник истины

Источник истины платформы - база состояния. Внешние системы остаются источниками истины для своих данных:

- билетная система - для билета и его действительности;
- сервис расписания - для рейса, платформы и статуса отправления;
- канал доставки - для факта показа уведомления в конкретном интерфейсе.

Платформа хранит снимок нужного контекста и отметку свежести данных, чтобы продолжать отвечать каналам при временной недоступности внешних зависимостей.

